@model E_Procurement.WebUI.Models.VendorCategoryModel.VendorCategoryModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Vendor Category";
    ViewBag.PageDesc = "Edit Category";
    ViewBag.FavIcon = "fa fa-street-view";
}

<!-- page section -->
<div class="container-fluid">
    <div class="row">
        <!-- basic forms -->
        <!-- forms cntrol -->
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            <div class="card">

                <div class="card-content">
                    <form class="form-horizontal" action="@Url.Action("Edit","VendorCategory")" method="post">
                        @Html.AntiForgeryToken()
                        @Html.HiddenFor(model => model.Id)

                        <br />
                        <fieldset>
                            <!-- Text input-->
                            <div class="form-group">
                                <label class="col-md-4 control-label">State Name <span class="text-danger">*</span></label>
                                <div class="col-md-6">
                                    <div class="input-field">
                                        @Html.EditorFor(model => model.CategoryName, new { htmlAttributes = new { id = "CategoryName", @class = "form-control", @type = "text", required = "required" } })
                                        @Html.ValidationMessageFor(model => model.CategoryName, "", new { @class = "text-danger" })
                                    </div>
                                </div>
                            </div>

                            <div class="form-group">
                                <label class="col-md-4 control-label">Is Active?</label>
                                <div class="col-md-6">
                                    <div class="input-field">
                                        @Html.CheckBoxFor(model => model.IsActive, new { Style = "opacity:10; left:0px;", htmlAttributes = new { id = "active", @type = "checkbox", @class = "form-control" } })
                                        @Html.ValidationMessageFor(model => model.IsActive, "", new { @class = "text-danger" })

                                    </div>
                                </div>
                            </div>
                            <br />
                            <!-- Button -->
                            <div class="form-group">
                                <label class="col-md-8 control-label"></label>
                                <div class="col-md-4">

                                    <button type="submit" class="btn btn-success"><span class="glyphicon glyphicon-send"></span>  Submit</button>

                                    <a href="@Url.Action("Index","VendorCategory")" class="btn btn-danger"><span class="glyphicon glyphicon-remove"></span> Back</a>
                                </div>

                            </div>
                            <br />
                        </fieldset>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <!-- ./bootstrap forms -->
</div>